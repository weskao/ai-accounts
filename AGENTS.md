# Repository Guidelines

Before starting any repository task, open and read [CLAUDE.md](CLAUDE.md) in full. Apply its instructions to Codex as if they were written directly in this file, including instructions addressed to Claude Code. Resolve its relative references from the directory containing `CLAUDE.md`. If it cannot be read, report the problem before proceeding.

Maintain all shared repository guidance in `CLAUDE.md` only. Do not copy or summarize it here. Modify `AGENTS.md` only when this delegation needs updating or an explicit Codex-specific exception is required; document any such exception narrowly.
